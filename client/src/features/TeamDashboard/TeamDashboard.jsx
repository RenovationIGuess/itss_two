import React from 'react';
import { Button } from '~/components/ui/button';

const TeamDashboard = () => {
  return (
    <div>
      <Button>Hover</Button>
    </div>
  );
};

export default TeamDashboard;
